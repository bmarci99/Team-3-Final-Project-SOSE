import os

from main import *


def People():
    number = 0
    if question_options.capitalize() == "Y":
        number = number + 1
    else:
        return
    return;


# this can be useful for the first person, later we can add a class with sqlalcemy